# Copy to .dev.vars to lock template writes locally, or set in Cloudflare:
# npx wrangler secret put ADMIN_PASSWORD
# Leave unset for open dev mode. Feed/render URLs stay private share-links either way.
ADMIN_PASSWORD=change-me-to-a-long-random-string
